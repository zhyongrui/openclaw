## Follow-Up Slice: Chatops Persistence And Retry Hardening

Continued real Feishu chatops validation around issue `#36` and fixed the next
runtime failures after command dispatch started working:

- hardened `src/integrations/openclaw-plugin/store.ts`
  - added in-process mutation serialization for read-modify-write state updates
  - switched atomic save temp files from a fixed `.tmp` path to unique temp
    filenames to avoid concurrent rename collisions
  - made read APIs wait for queued mutations so snapshots are not observed
    mid-write inside the same process
- added a concurrent regression test in
  `src/openclawcode/testing/openclaw-plugin-store.test.ts` proving parallel
  workflow status writes no longer drop snapshots
- reduced plugin load-time coupling in `extensions/openclawcode/index.ts` by
  lazy-loading `message-action-runner` only when a chat notification is actually
  sent
- changed `src/integrations/openclaw-plugin/chatops.ts` so background issue
  runs now invoke the already-built `dist/index.js` entrypoint directly instead
  of routing through `scripts/run-node.mjs`
- updated `src/openclawcode/testing/openclaw-plugin-integration.test.ts` for
  the new argv contract

Validated the hardening locally with:

- `pnpm exec vitest run src/openclawcode/testing/openclaw-plugin-store.test.ts --pool threads`
- `pnpm exec vitest run extensions/openclawcode/index.test.ts --pool threads`
- `pnpm exec vitest run src/openclawcode/testing/openclaw-plugin-integration.test.ts extensions/openclawcode/index.test.ts src/openclawcode/testing/openclaw-plugin-store.test.ts --pool threads`
- `pnpm exec vitest run --config vitest.openclawcode.config.mjs`
- `pnpm build`

Then revalidated the real chatops path against issue `#36`:

- restarted the local repo gateway from `/home/zyr/pros/openclawcode`
- manually re-enqueued `zhyongrui/openclawcode#36` into the plugin queue to
  avoid waiting for another human DM round-trip
- observed the workflow finish and persist:
  - run record `zhyongrui-openclawcode-36-1773154815154.json`
  - final status `ready-for-human-review`
  - draft PR `https://github.com/zhyongrui/openclawcode/pull/37`

One cleanup note from this validation:

- the manual out-of-process requeue used for fast recovery left a transient
  stale `currentRun` entry in plugin state after the successful result snapshot
  had already been written
- this was repaired immediately by finishing the run with the persisted final
  status message
- the normal user path still writes only from the gateway process, so the
  repaired state now reflects the intended steady-state outcome for issue `#36`
